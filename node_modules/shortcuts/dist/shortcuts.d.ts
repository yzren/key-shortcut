import { Shortcut as ShortcutType, ShortcutID, RecordHandler, ShortcutsOptions, ShortcutDescriptor } from './types';
declare class Shortcuts {
    private listener;
    private descriptors;
    private shortcuts;
    private recordHandler;
    constructor(options?: ShortcutsOptions);
    private _updateListener;
    get(): ShortcutDescriptor[];
    add(descriptors: ShortcutDescriptor | ShortcutDescriptor[]): any;
    remove(descriptors: ShortcutDescriptor | ShortcutDescriptor[]): any;
    reset(): void;
    record(handler: RecordHandler): Function;
    trigger(shortcut: ShortcutType | ShortcutID): boolean;
    private handler;
}
export default Shortcuts;
