import { ListenerOptions } from './types';
declare class Listener {
    private options;
    private capture;
    private target;
    private shouldHandleEvent;
    private currentKeydownShortcutID;
    private currentKeypressShortcutID;
    private resetNextKeydownShortcutID;
    private triggeredNextKeypress;
    private ignoreNextKeypress;
    private listening;
    constructor(options: ListenerOptions);
    on(): void;
    off(): void;
    isListening(): boolean;
    handler: (event: KeyboardEvent) => void;
}
export default Listener;
